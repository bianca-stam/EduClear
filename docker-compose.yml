services:
  usuario-service:
    build: ./educlear-service-Backend/usuario-service
    environment:
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}

  curso-service:
    build: ./educlear-service-Backend/curso-service
    environment:
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}

  frontend:
    build: ./educlear-frontend